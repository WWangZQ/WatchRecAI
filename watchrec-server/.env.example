# Optional for source users. UI settings take priority after you save them.
# You can leave everything blank and configure the app in its window.
VPS_BASE_URL=
APP_TOKEN=
PORT=18765
LAN_ENABLED=false
LAN_IP_OVERRIDE=
ASR_DEVICE=auto
TIMEZONE=Asia/Shanghai

# Optional AI text processing; audio transcription itself does not need an API key.
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=
